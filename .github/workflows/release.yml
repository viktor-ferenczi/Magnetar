name: Release

# Builds Magnetar for Linux and Windows and publishes a GitHub release with
# both .7z bundles attached.
#
#   * push to main      -> read the version from Directory.Build.props, and only
#                          build + publish a public "latest" release when that
#                          version is strictly higher than the latest existing
#                          release (the first ever release always counts as
#                          newer). Otherwise the whole run is skipped.
#   * manual dispatch   -> always build for the current version,
#                          regardless of what is already released. The "draft"
#                          input (default true) decides whether the result is a
#                          DRAFT build (not marked latest) or a real, public
#                          "latest" release.
#
# Required repository configuration:
#   * secrets.VENDOR_ARCHIVE_URL - download URL that returns Vendor.7z, containing
#                                the Vendor/ folder with libsteam_api.so and
#                                libEOSSDK-Linux-Shipping.so (the proprietary
#                                blobs that can't be committed).

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      draft:
        description: "Publish as a DRAFT build (uncheck to publish a real, public 'latest' release)"
        type: boolean
        default: true

permissions:
  contents: write

# Steam app id of the Space Engineers Dedicated Server (free, anonymous login).
env:
  SE_DS_APPID: "298740"

jobs:
  # ---------------------------------------------------------------------------
  # Gate: decide whether to build, what tag to use, and draft vs latest.
  # ---------------------------------------------------------------------------
  version-check:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.decide.outputs.version }}
      should_build: ${{ steps.decide.outputs.should_build }}
      draft: ${{ steps.decide.outputs.draft }}
      ds_buildid: ${{ steps.dsbuild.outputs.buildid }}
    steps:
      - uses: actions/checkout@v6

      - name: Decide release plan
        id: decide
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          # Version from Directory.Build.props (single source of truth for the tag).
          version="$(grep -oPm1 '(?<=<Version>)[^<]+' Directory.Build.props)"
          if [ -z "$version" ]; then
            echo "ERROR: could not parse <Version> from Directory.Build.props" >&2
            exit 1
          fi
          echo "Magnetar version: $version"

          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            # Manual run: always build (the operator explicitly asked for it).
            # The "draft" input decides draft vs. real "latest" release; it
            # defaults to true, preserving the safe build-only behaviour.
            draft="${{ inputs.draft }}"
            echo "Manual dispatch -> unconditional build (draft=$draft)."
            should_build=true
          else
            # Push to main: build only if strictly newer than the latest release.
            # /releases/latest returns HTTP 404 (and a JSON error body on stdout)
            # when no release exists yet, so only trust the output on success.
            latest=""
            if tag="$(gh api "repos/${{ github.repository }}/releases/latest" \
                        --jq '.tag_name' 2>/dev/null)"; then
              latest="${tag#v}"
            fi

            if [ -z "$latest" ]; then
              echo "No existing release -> first release counts as newer."
              should_build=true
            elif [ "$version" = "$latest" ]; then
              echo "Version $version equals latest release $latest -> skip."
              should_build=false
            elif [ "$(printf '%s\n%s\n' "$version" "$latest" | sort -V | tail -1)" = "$version" ]; then
              echo "Version $version > latest release $latest -> build."
              should_build=true
            else
              echo "Version $version <= latest release $latest -> skip."
              should_build=false
            fi
            draft=false
          fi

          {
            echo "version=$version"
            echo "should_build=$should_build"
            echo "draft=$draft"
          } >> "$GITHUB_OUTPUT"

      # The DS depot build id keys the cached library set: when Keen ships a new
      # DS version the build id changes, so the next release build misses the
      # cache and re-fetches; an unchanged version is restored instantly. Probed
      # cheaply via app_info_print (no depot download). Only when building.
      - name: Probe Dedicated Server build id
        id: dsbuild
        if: steps.decide.outputs.should_build == 'true'
        run: |
          set -euo pipefail
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends lib32gcc-s1 ca-certificates curl
          mkdir -p steamcmd
          curl -fsSL https://steamcdn-a.akamaihd.net/client/installer/steamcmd_linux.tar.gz \
            | tar -xz -C steamcmd

          # Parse branches.public.buildid out of the appinfo VDF dump. awk keeps
          # this inline (a python heredoc's column-0 body would break the YAML
          # block scalar); the line is `"buildid"  "<n>"`, so strip non-digits.
          buildid=""
          for attempt in 1 2 3 4 5; do
            out="$(./steamcmd/steamcmd.sh +login anonymous +app_info_update 1 \
                     +app_info_print "$SE_DS_APPID" +quit 2>/dev/null || true)"
            buildid="$(printf '%s' "$out" | tr -d '\r' | awk '
              /"branches"/              { in_branches = 1 }
              in_branches && /"public"/ { in_public = 1 }
              in_public && /"buildid"/  { gsub(/[^0-9]/, "", $2); print $2; exit }
            ')"
            [ -n "$buildid" ] && break
            echo "build id probe attempt $attempt empty; retrying..."
            sleep 5
          done

          if [ -z "$buildid" ]; then
            # Don't let a probe hiccup wedge the release: fall back to a unique
            # key so the DS cache simply misses (full download) this run.
            echo "WARNING: could not determine DS build id; DS cache will miss this run." >&2
            buildid="norev-${{ github.run_id }}"
          fi
          echo "DS public build id: $buildid"
          echo "buildid=$buildid" >> "$GITHUB_OUTPUT"

  # ---------------------------------------------------------------------------
  # Linux build: build.sh -> dist/MagnetarForLinux.7z
  # ---------------------------------------------------------------------------
  build-linux:
    needs: version-check
    if: needs.version-check.outputs.should_build == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install .NET SDKs
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Install build tools (7z)
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends p7zip-full ca-certificates curl

      # --- se-linux-compat native wrappers (cached by upstream commit) -------
      - name: Resolve se-linux-compat commit
        id: linuxcompat
        run: |
          set -euo pipefail
          sha="$(git ls-remote https://github.com/viktor-ferenczi/se-linux-compat HEAD | cut -f1)"
          echo "Latest se-linux-compat commit: $sha"
          echo "sha=$sha" >> "$GITHUB_OUTPUT"

      - name: Cache native wrappers
        id: nativecache
        uses: actions/cache@v5
        with:
          path: native
          key: nativewrappers-${{ steps.linuxcompat.outputs.sha }}

      - name: Build native wrappers (Docker)
        if: steps.nativecache.outputs.cache-hit != 'true'
        run: |
          set -euo pipefail
          rm -rf se-linux-compat
          git clone https://github.com/viktor-ferenczi/se-linux-compat se-linux-compat
          git -C se-linux-compat checkout "${{ steps.linuxcompat.outputs.sha }}"
          DOCKER_BUILDKIT=1 docker build \
            -f .github/docker/nativewrappers.Dockerfile \
            -o type=local,dest=native \
            se-linux-compat/NativeWrappers
          echo "Built native wrappers:"
          ls -l native

      # --- Vendor proprietary blobs (always downloaded, never cached) --------
      - name: Download Vendor archive
        env:
          VENDOR_ARCHIVE_URL: ${{ secrets.VENDOR_ARCHIVE_URL }}
        run: |
          set -euo pipefail
          if [ -z "${VENDOR_ARCHIVE_URL:-}" ]; then
            echo "ERROR: repository secret VENDOR_ARCHIVE_URL is not set." >&2
            exit 1
          fi
          curl -fsSL "$VENDOR_ARCHIVE_URL" -o Vendor.7z
          rm -rf Vendor
          7z x -y Vendor.7z >/dev/null
          echo "Vendor contents:"
          ls -l Vendor

      # --- Dedicated Server libraries (cached by DS build id) ----------------
      # Only the ~186 MB DedicatedServer64/ library set is cached, not the
      # multi-GB Content. Restored instantly when the DS version is unchanged;
      # re-fetched on the first build after Keen ships a new DS version.
      - name: Cache Dedicated Server libraries
        id: dscache
        uses: actions/cache@v5
        with:
          path: ds64
          key: ds64-linux-${{ needs.version-check.outputs.ds_buildid }}

      # --- Space Engineers Dedicated Server (Windows depot, via steamcmd) ----
      - name: Download Dedicated Server (steamcmd)
        if: steps.dscache.outputs.cache-hit != 'true'
        run: |
          set -euo pipefail
          # steamcmd is a 32-bit binary; install its runtime only on a cache miss.
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends lib32gcc-s1
          mkdir -p steamcmd dsfull
          curl -fsSL https://steamcdn-a.akamaihd.net/client/installer/steamcmd_linux.tar.gz \
            | tar -xz -C steamcmd

          # A brand-new steamcmd self-updates on its first run, which makes an
          # in-session app_update abort with "Missing configuration". Bootstrap
          # once, then retry the install (steamcmd is also flaky enough in CI
          # that a couple of retries are worthwhile). SE DS has no native Linux
          # depot, so force the Windows files (run via the native wrappers).
          ./steamcmd/steamcmd.sh +quit || true

          ok=0
          for attempt in 1 2 3 4 5; do
            ./steamcmd/steamcmd.sh \
              +@sSteamCmdForcePlatformType windows \
              +force_install_dir "$PWD/dsfull" \
              +login anonymous \
              +app_update "$SE_DS_APPID" validate \
              +quit || true
            if [ -f "$PWD/dsfull/DedicatedServer64/SpaceEngineersDedicated.exe" ]; then
              ok=1; break
            fi
            echo "steamcmd attempt $attempt did not complete the DS install; retrying..."
            sleep 5
          done
          if [ "$ok" != 1 ]; then
            echo "ERROR: steamcmd failed to install the Dedicated Server." >&2
            exit 1
          fi

          # Keep only the library set so just ~186 MB gets cached; drop Content.
          # ds64/ is populated only after the marker check passes, so a failed
          # download never caches a partial/empty tree.
          mkdir -p ds64
          cp -a "$PWD/dsfull/DedicatedServer64/." ds64/
          rm -rf dsfull
          echo "Cached DS library set:"; du -sh ds64

      - name: Build and package (build.sh)
        env:
          DS64: ${{ github.workspace }}/ds64
          LIBHAVOK_SO: ${{ github.workspace }}/native/libHavok.so
          LIBRECASTDETOUR_SO: ${{ github.workspace }}/native/libRecastDetour.so
          LIBVRAGENATIVE_SO: ${{ github.workspace }}/native/libVRageNative.so
          VERSION: ${{ needs.version-check.outputs.version }}
        run: |
          set -euo pipefail
          chmod +x build.sh Scripts/*.sh
          ./build.sh
          # build.sh names the bundle dist/MagnetarForLinux.7z;
          # rename it to the version-stamped release name.
          mv dist/MagnetarForLinux.7z "dist/MagnetarForLinux-${VERSION}.7z"
          echo "Produced:"
          ls -l "dist/MagnetarForLinux-${VERSION}.7z"

      - name: Upload Linux bundle
        uses: actions/upload-artifact@v7
        with:
          name: magnetar-linux
          path: dist/MagnetarForLinux-*.7z
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # Windows build: build Magnetar.sln (deploy.bat stages the install tree) ->
  # MagnetarForWindows.<date>.<hash>.7z
  # ---------------------------------------------------------------------------
  build-windows:
    needs: version-check
    if: needs.version-check.outputs.should_build == 'true'
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install .NET 10 SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      # --- Dedicated Server libraries (cached by DS build id) ----------------
      - name: Cache Dedicated Server libraries
        id: dscache
        uses: actions/cache@v5
        with:
          path: ds64
          key: ds64-windows-${{ needs.version-check.outputs.ds_buildid }}

      # --- Space Engineers Dedicated Server (via steamcmd) -------------------
      - name: Download Dedicated Server (steamcmd)
        if: steps.dscache.outputs.cache-hit != 'true'
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          New-Item -ItemType Directory -Force -Path steamcmd, dsfull | Out-Null
          Invoke-WebRequest -Uri "https://steamcdn-a.akamaihd.net/client/installer/steamcmd.zip" -OutFile steamcmd.zip
          Expand-Archive -Path steamcmd.zip -DestinationPath steamcmd -Force
          $dsfull = Join-Path $env:GITHUB_WORKSPACE "dsfull"
          $marker = Join-Path $dsfull "DedicatedServer64\SpaceEngineersDedicated.exe"

          # A brand-new steamcmd self-updates on its first run, which makes an
          # in-session app_update abort with "Missing configuration". Bootstrap
          # once, then retry the install (steamcmd is flaky enough in CI that a
          # couple of retries are worthwhile).
          & steamcmd/steamcmd.exe +quit | Out-Null

          $ok = $false
          for ($attempt = 1; $attempt -le 5; $attempt++) {
            & steamcmd/steamcmd.exe +force_install_dir "$dsfull" +login anonymous +app_update $env:SE_DS_APPID validate +quit
            if (Test-Path $marker) { $ok = $true; break }
            Write-Host "steamcmd attempt $attempt did not complete the DS install; retrying..."
            Start-Sleep -Seconds 5
          }
          if (-not $ok) { throw "steamcmd failed to install the Dedicated Server." }

          # Keep only the library set so just ~186 MB gets cached; drop Content.
          $ds64 = Join-Path $env:GITHUB_WORKSPACE "ds64"
          New-Item -ItemType Directory -Force -Path $ds64 | Out-Null
          Copy-Item -Path (Join-Path $dsfull "DedicatedServer64\*") -Destination $ds64 -Recurse -Force
          Remove-Item -Recurse -Force $dsfull

      - name: Build and stage (Magnetar.sln)
        shell: pwsh
        env:
          DS64: ${{ github.workspace }}\ds64
          # deploy.bat (post-build) stages the install tree here instead of
          # %APPDATA%\Magnetar. The leaf folder name becomes the archive root.
          Magnetar: ${{ github.workspace }}\stage\Magnetar
        run: |
          $ErrorActionPreference = "Stop"
          dotnet build -c Release Magnetar.sln
          Write-Host "Staged install tree:"
          Get-ChildItem -Recurse "$env:GITHUB_WORKSPACE\stage\Magnetar" -Depth 1 | Select-Object FullName

      - name: Package Windows bundle
        shell: pwsh
        env:
          VERSION: ${{ needs.version-check.outputs.version }}
        run: |
          $ErrorActionPreference = "Stop"
          $name = "MagnetarForWindows-$env:VERSION.7z"
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          $sevenzip = "C:\Program Files\7-Zip\7z.exe"
          # Pack the staged Magnetar/ folder so the archive root mirrors the
          # %APPDATA%\Magnetar install layout deploy.bat produces.
          & $sevenzip a -t7z -mx=9 "dist\$name" "$env:GITHUB_WORKSPACE\stage\Magnetar"
          Get-ChildItem dist

      - name: Upload Windows bundle
        uses: actions/upload-artifact@v7
        with:
          name: magnetar-windows
          path: dist/MagnetarForWindows-*.7z
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # Release: attach both bundles to a GitHub release.
  # ---------------------------------------------------------------------------
  release:
    needs: [version-check, build-linux, build-windows]
    if: needs.version-check.outputs.should_build == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Download bundles
        uses: actions/download-artifact@v8
        with:
          path: dist
          merge-multiple: true

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ needs.version-check.outputs.version }}
          DRAFT: ${{ needs.version-check.outputs.draft }}
        run: |
          set -euo pipefail
          echo "Artifacts to attach:"
          ls -l dist/*.7z

          tag="v$VERSION"
          if [ "$DRAFT" = "true" ]; then
            # Keep the tag unique so a manual draft never collides with an
            # existing release of the same version.
            if gh release view "$tag" >/dev/null 2>&1; then
              tag="v$VERSION-build.${{ github.run_number }}"
            fi
            gh release create "$tag" \
              --repo "${{ github.repository }}" \
              --draft \
              --title "Magnetar $tag" \
              --notes "Manual draft build of Magnetar $VERSION." \
              dist/*.7z
          else
            gh release create "$tag" \
              --repo "${{ github.repository }}" \
              --latest \
              --target "${{ github.sha }}" \
              --title "Magnetar v$VERSION" \
              --notes "Automated release of Magnetar $VERSION." \
              dist/*.7z
          fi
